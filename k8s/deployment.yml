apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: php-ingress
  namespace: prakai-project-01
  annotations:
    kubernetes.io/ingress.class: "nginx"
    # This rewrite rule is CRITICAL for sub-paths
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: dev.cpe.rmuti.ac.th
    http:
      paths:
      - path: /prakai-project-0/
        pathType: Prefix
        backend:
          service:
            name: php-service
            port:
              number: 80
